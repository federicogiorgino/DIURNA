import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    borderRadius: 20,
    padding: 3,
  },
  leftTile: { flexDirection: "row", alignItems: "center" },
});
